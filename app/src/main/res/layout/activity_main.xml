<?xml version="1.0" encoding="utf-8"?>
<android.support.wear.widget.BoxInsetLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/dark_grey"
    android:padding="@dimen/box_inset_layout_padding"
    tools:context=".MainActivity"
    tools:deviceIds="wear">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:boxedEdges="all"
        tools:ignore="MissingPrefix">
        <!--"tools:ignore=...", siehe https://stackoverflow.com/a/53686106/1364368 -->

        <Button
            android:id="@+id/ortungsButton"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:textSize="20sp"
            android:gravity="center_horizontal|center_vertical"
            android:text="Ortung!" />


        <!-- Fortschrittsanzeige, ist zunächst unsichtbar, wird nur während Ortungs-Vorgang
             auf sichtbar geschaltet. -->
        <ProgressBar
            android:id="@+id/fortschrittsanzeige"
            android:visibility="invisible"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:gravity="center_horizontal|center_vertical" />

    </FrameLayout>

</android.support.wear.widget.BoxInsetLayout>